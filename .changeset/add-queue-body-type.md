---
'@observerly/orderly': minor
---

Add the QueueBody type: the body a defineQueue() contract carries, taken from the contract itself, so the shape is named once in the schema and read from the contract everywhere else.
