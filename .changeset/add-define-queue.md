---
'@observerly/orderly': minor
---

Add defineQueue() to the queues module: the shared contract declaring the payload type and schema once, with the producing and consuming ends derived from it so neither side can drift alone.
