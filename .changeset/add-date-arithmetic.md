---
'@observerly/orderly': minor
---

Add add() and subtract() to the schedules module: a new instant a span later or earlier, never mutating the Date given, refusing invalid and out-of-range instants loudly.
