---
'@observerly/orderly': minor
---

Add the StandardSchema type family to the schema module: the Standard Schema v1 interface declared structurally, so any conforming validation library is accepted without orderly depending on one.
