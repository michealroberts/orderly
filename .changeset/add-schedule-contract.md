---
'@observerly/orderly': minor
---

Add the Schedule contract to the schedules module: one method, next(after), returning the first occurrence strictly after the instant given, or null once exhausted.
