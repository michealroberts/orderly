---
'@observerly/orderly': minor
---

Add at().once() to the schedules module: runs once at exactly the instant given, to the millisecond, then exhausts. at() alone is an anchor, not a schedule, so an unfinished sentence does not compile.
