---
'@observerly/orderly': minor
---

Add union() to the schedules module: the earliest next occurrence across several schedules, firing whenever any member does and exhausting only once every member has.
