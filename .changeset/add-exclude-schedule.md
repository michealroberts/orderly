---
'@observerly/orderly': minor
---

Add exclude() to the schedules module: a schedule less the occurrences another schedule names, matched exactly to the millisecond, the way RFC 5545 removes exception dates from a recurrence set.
