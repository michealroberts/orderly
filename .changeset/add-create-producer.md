---
'@observerly/orderly': minor
---

Add createProducer() to the queues module: the typed producing end, with delays clamped to the platform bounds and an optional schema that refuses to enqueue a bad body.
