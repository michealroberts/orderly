---
'@observerly/orderly': minor
---

Add every() to the schedules module: fixed cadences in minutes or hours, aligned to the Unix epoch so every worker on every replay computes the same series, refusing counts whose schedules could never advance.
